<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AssemblyVersion>2.0.0.9</AssemblyVersion>
    <FileVersion>2.0.0.9</FileVersion>
    <Version>2.0.0-alpha9</Version>
    <Authors>AluminiumTech</Authors>
    <Company />
    <Description>PlatformKit is a library dealing with Platform specific code, platform specific issues, and platform detection in C# .NET Core.</Description>
    <Copyright>Copyright (c) AluminiumTech 2017-2020</Copyright>
    <RepositoryUrl>https://github.com/AluminiumTech/PlatformKit</RepositoryUrl>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <PackageLicenseFile>LICENSE.txt</PackageLicenseFile>
    <RepositoryType>git</RepositoryType>
    <RootNamespace>AluminiumTech.PlatformKit</RootNamespace>
    <AssemblyName>AluminiumCoreLib</AssemblyName>
    <PackOnBuild>true</PackOnBuild>
    <PackageId>PlatformKit</PackageId>
    <PackageVersion>2.0.0-beta4</PackageVersion>
    <Owners>AluminiumTech</Owners>
    <Summary>PlatformKit is a library dealing with Platform specific code, platform specific issues, and platform detection in C# .NET Core.</Summary>
    <PackageTags>platform detection, CSharp, </PackageTags>
    <Title>PlatformKit</Title>
    <PackageReleaseNotes>### Changes
* Fixed another issue with ``ToEnum()`` on macOS 

### Breaking Changes
* Removed some redundant Kernel related detection code
* Deprecated macOS and Windows Kernel detection code in the ``Platform`` class - This will eventually be moved to new classes and re-written similar to how it has been done for the new ``LinuxPlatform`` class
* Moved Linux Kernel related detection code to new ``LinuxPlatform`` class
* Revamped detection for Linux Kernel Version
* Added detection for Linux Desktop Environment
* Added detection for Linux Distribution
* Added detection for Linux Distribution Version
* Added the ability to get information from uname and lsb_release from C#
* Added the ability to get the full Linux Kernel information from uname</PackageReleaseNotes>
  </PropertyGroup>

  <ItemGroup>
    <None Remove="LICENSE.txt" />
    <None Remove="PlatformKitTest\**" />
    <None Remove="TestConsole\**" />
  </ItemGroup>
  <ItemGroup>
    <None Include="LICENSE.txt">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <ItemGroup>
    <Compile Update="PlatformKit\FolderManager.cs">
      <IncludeInPackage>false</IncludeInPackage>
    </Compile>
    <Compile Remove="PlatformKitTest\**" />
    <Compile Remove="TestConsole\**" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Remove="PlatformKitTest\**" />
    <EmbeddedResource Remove="TestConsole\**" />
  </ItemGroup>
</Project>