<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AssemblyVersion>2.0.0.4</AssemblyVersion>
    <FileVersion>2.0.0.4</FileVersion>
    <Version>2.0.0-alpha4</Version>
    <Authors>AluminiumTech</Authors>
    <Company />
    <Description>AluminiumCoreLib is designed to help programmers be more efficient in C# by containing things that programmers should have and/or may need in their application.</Description>
    <PackageLicenseUrl>https://github.com/AluminiumTech/AluminiumCoreLib/blob/master/LICENSE</PackageLicenseUrl>
    <Copyright>Copyright (c) AluminiumTech 2017-2018</Copyright>
    <RepositoryUrl>https://github.com/AluminiumTech/AluminiumCoreLib</RepositoryUrl>
    <PackageReleaseNotes>## Changes

### Both

### Java lib
* Added native ``toString()`` method to JVersion
* Added ``String suffix`` to JVersion and methods to access and modify it
* Added constructor for JVersion which uses multiple Strings instead of multiple int variables
* Added constructor for JVersion which uses a single String 
* Added VersionComparison class to help compare versions
* Update to Java 11

### C Sharp lib
* Removed most Compare methods in VersionComparison class and made ``Compare()`` base method public
* Created AluminiumCoreLib.IO library and moved IO stuff into that lib - This should result in a substantially smaller nuget download size for the regular library.
* API compatibility changes with IO classes - Instead of relying on AluminiumCoreLib, one of the IO classes hasbeen modified to use C#'s native ``List&lt;&gt;`` class instead of ``ObjectList&lt;&gt;``
* Minor Tweaks to some enums in ``Platform.cs``</PackageReleaseNotes>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <PackageProjectUrl></PackageProjectUrl>
  </PropertyGroup>

</Project>