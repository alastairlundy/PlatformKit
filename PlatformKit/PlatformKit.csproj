<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <Authors>Alastair Lundy</Authors>
        <Company />
        <Description>Cross-platform Operating System Detection, Operating System Version Detection, Apple Silicon Mac Detection, programmatic .NET Runtime ID Generation, and more. For a full list of features please visit: https://github.com/NeverSpyTech/PlatformKit/blob/main/docs/FeatureComparisonByPlatform.md</Description>
        <Copyright>Copyright (c) Alastair Lundy 2018-2023</Copyright>
        <RepositoryUrl>https://github.com/alastairlundy/PlatformKit/</RepositoryUrl>
        <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
        <RepositoryType>git</RepositoryType>
        <PackOnBuild>true</PackOnBuild>
        <PackageId>PlatformKit</PackageId>
        <Owners>AlastairLundy</Owners>
        <Summary>PlatformKit is a library dealing with Platform specific code, platform specific issues, and platform detection in C# .NET Core.</Summary>
        <PackageTags>platform detection, CSharp,</PackageTags>
        <Title>PlatformKit</Title>
        <PackageReleaseNotes>### Changes since 3.5.0
* Added Build numbers assignments to WindowsVersion enum values
    *  Note: Windows 10 Insider Preview and Windows 11 Insider Preview as well as Not Supported and Not Detected enumerated values are not assigned any build numbers.
* Deprecated OSAnalyzer ``IsWindows()``, ``IsMac()``, and ``IsLinux()`` methods - These methods will be available only for projects targeting .NET Standard 2.0 or .NET Standard 2.1`` in PlatformKit 4 - This is to ensure that projects which do not have access to the newer OperatingSystem APIs on .NET 5 and onwards can continue to receive access to these methods and this functionality. - Please use the OperatingSystem APIs on .NET 5 if your project is targeting .NET 5 or newer.
* Deprecated ``ConvertStringToProcess`` method. This method will be replaced by the ``GetProcessesFromProcessName`` method which has identical functionality
* Replaced the exception type thrown when attempting to detect a FreeBSD version on an OS which is not FreeBSD

### Notes
* Final minor release of PlatformKit 3 - Bug fixes and Security Updates will continue if required in accordance with the PlatformKit Security Policy.
* PlatformKit 4 Pre-release builds are expected to commence within a few weeks</PackageReleaseNotes>
   <Product>PlatformKit</Product>
        <LangVersion>default</LangVersion>
        <AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageLicenseFile>LICENSE.txt</PackageLicenseFile>
        <TargetFrameworks>net6.0;netstandard2.0</TargetFrameworks>
   <AssemblyVersion>3.6.0.0</AssemblyVersion>
        <FileVersion>3.6.0.0</FileVersion>
        <Version>3.6.0</Version>
        <PackageVersion>3.6.0</PackageVersion>
        <PackageProjectUrl>https://github.com/alastairlundy/PlatformKit/</PackageProjectUrl>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <PackageIcon>platformkit_logo_512x.jpg</PackageIcon>
        <IncludeContentInPack>true</IncludeContentInPack>
    </PropertyGroup>

    <ItemGroup>
        <None Include="..\images\logos\platformkit_logo_512x.jpg">
          <Pack>True</Pack>
          <PackagePath></PackagePath>
          <Link>platformkit_logo_512x.jpg</Link>
        </None>
        
        <None Include="../README.md" Pack="true" PackagePath="\" />
    </ItemGroup>

    <ItemGroup>
      <Folder Include="Platforms" />
    </ItemGroup>

    <ItemGroup>
      <Content Include="..\LICENSE.txt">
        <Link>LICENSE.txt</Link>
          <PackagePath>LICENSE.txt</PackagePath>
          <CopyToOutputDirectory>Always</CopyToOutputDirectory>
      </Content>
    </ItemGroup>
</Project>